name: Temporary Pack Flow Verify

on:
  push:
    branches:
      - feature/add-equipment-cards-v2

permissions:
  contents: read
  statuses: write

jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - id: install
        continue-on-error: true
        run: npm install
      - id: focused
        run: |
          set +e
          npx vitest run src/game/workoutCompletionPack.test.ts src/screens/WorkoutCompletionFeedback.test.tsx > focused.log 2>&1
          code=$?
          if [ "$code" = "0" ]; then
            summary=$(grep -E "Test Files|Tests " focused.log | tail -n 2 | tr '\n' ' ')
          else
            summary=$(grep -E "AssertionError|TestingLibraryElementError|Unable to find|Found multiple|expected|Error:" focused.log | head -n 1)
          fi
          if [ -z "$summary" ]; then summary=$(tail -n 6 focused.log | tr '\n' ' '); fi
          echo "code=$code" >> "$GITHUB_OUTPUT"
          echo "summary_b64=$(printf '%s' "$summary" | base64 -w0)" >> "$GITHUB_OUTPUT"
          exit 0
      - id: fulltests
        run: |
          set +e
          npm test > full.log 2>&1
          code=$?
          if [ "$code" = "0" ]; then
            summary=$(grep -E "Test Files|Tests " full.log | tail -n 2 | tr '\n' ' ')
          else
            summary=$(grep -E "AssertionError|TestingLibraryElementError|Unable to find|Found multiple|expected|Error:" full.log | head -n 1)
          fi
          if [ -z "$summary" ]; then summary=$(tail -n 8 full.log | tr '\n' ' '); fi
          echo "code=$code" >> "$GITHUB_OUTPUT"
          echo "summary_b64=$(printf '%s' "$summary" | base64 -w0)" >> "$GITHUB_OUTPUT"
          exit 0
      - id: build
        continue-on-error: true
        run: npm run build
      - name: Publish verification statuses
        if: always()
        uses: actions/github-script@v7
        env:
          FOCUSED_CODE: ${{ steps.focused.outputs.code }}
          FOCUSED_SUMMARY_B64: ${{ steps.focused.outputs.summary_b64 }}
          FULL_CODE: ${{ steps.fulltests.outputs.code }}
          FULL_SUMMARY_B64: ${{ steps.fulltests.outputs.summary_b64 }}
        with:
          script: |
            const decode = (value) => value
              ? Buffer.from(value, 'base64').toString('utf8').replace(/\u001b\[[0-9;]*m/g, '').replace(/\s+/g, ' ').slice(0, 70)
              : 'no-output';
            const focusedSummary = decode(process.env.FOCUSED_SUMMARY_B64);
            const fullSummary = decode(process.env.FULL_SUMMARY_B64);
            const checks = [
              ['pack-flow-install', '${{ steps.install.outcome }}'],
              [`pack-flow-focused:${focusedSummary}`, process.env.FOCUSED_CODE === '0' ? 'success' : 'failure'],
              [`pack-flow-full:${fullSummary}`, process.env.FULL_CODE === '0' ? 'success' : 'failure'],
              ['pack-flow-build', '${{ steps.build.outcome }}'],
            ];
            for (const [name, outcome] of checks) {
              const state = outcome === 'success' ? 'success' : 'failure';
              await github.rest.repos.createCommitStatus({
                owner: context.repo.owner,
                repo: context.repo.repo,
                sha: context.sha,
                state,
                context: name,
                description: `${name}: ${outcome}`.slice(0, 140),
              });
            }
      - name: Fail when verification failed
        if: always()
        run: |
          test "${{ steps.install.outcome }}" = "success"
          test "${{ steps.focused.outputs.code }}" = "0"
          test "${{ steps.fulltests.outputs.code }}" = "0"
          test "${{ steps.build.outcome }}" = "success"
